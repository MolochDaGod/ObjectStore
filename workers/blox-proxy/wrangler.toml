# blox.grudge-studio.com — GrudgeBlox Next.js origin is Vercel project `grudgeblox`.
# Game WS stays on Railway (grudgeblox-production.up.railway.app). No player DB here.
name = "grudgeblox-edge-proxy"
main = "index.js"
compatibility_date = "2026-08-01"
workers_dev = true
account_id = "ee475864561b02d4588180b8b9acf694"

routes = [
  { pattern = "blox.grudge-studio.com", custom_domain = true }
]
