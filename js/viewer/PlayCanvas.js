/**
 * PlayCanvas.js — Stub: scene management is handled by the inline viewer in 3D_MODELS.html
 * Kept for API compatibility — the viewer exposes window._3dViewer directly.
 * Uses global BABYLON namespace (UMD CDN build)
 */
class PlayCanvas {
  constructor() {
    console.info('[PlayCanvas] Scene management handled by inline BJS viewer');
  }
}
window.PlayCanvas = PlayCanvas;
