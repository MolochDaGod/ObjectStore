# Vite / Node build artefacts — `dist/` is the hand-curated static-CDN deploy
# (don't ignore it), but `dist-build/` is the transient vite output.
node_modules/
dist-build/

# Editor/OS noise
.DS_Store
*.log
.vite/
