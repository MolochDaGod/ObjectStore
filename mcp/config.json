{
  "$schema": "https://raw.githubusercontent.com/anthropics/model-context-protocol/main/schema/config.json",
  "mcpServers": {
    "objectstore": {
      "command": "node",
      "args": ["./mcp/objectstore-server.js"],
      "description": "Grudge Studio ObjectStore - Game data API for weapons, armor, sprites, enemies, and more"
    }
  }
}
